<div class="container is-max-desktop" id="signup">
    <div class=" section signup-form">
        <h2 class="title">Sign Up</h2>
        <form id="signup-form">
            <div class="field">
                <label class="label" for="email-signup">Email:</label>
                <input class="input" type="text" id="email-signup" required />
            </div>
            <div class="field">
                <label class="label" for="password-signup">Password:</label>
                <input class="input" type="password" id="password-signup" required />
            </div>
            <div class="field">
                <button class="button" type="submit">Signup</button>
            </div>
                  <div class="field">
        <a href="/login">Log In Instead</a>
      </div>
        </form>
    </div>
</div>